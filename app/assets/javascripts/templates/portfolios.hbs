<div class="span3">
  <div class="well sidebar-nav">
    <ul class="nav nav-list">
      <li class="nav-header">Portfolios ({{content.length}})
	</li>
      {{#each portfolio in controller}}
        {{view HelloEmber.PortfolioInListView contentBinding="portfolio"}}
      {{/each}}
    </ul>
  </div>
</div>

<div class="span8">

<div id="flash" class="alert alert-success">
    <span></span>
</div>

<p class="box">Portfolios {{#linkTo "portfolios.new" class="btn btn-mini" }}<i class="icon-plus-sign"></i> Add Portfolio{{/linkTo}}</p>

	{{outlet}}
	
	{{#if controller.display_list}}
		<table class="table table-condensed table-bordered table-striped">
		<tr>
			<th>Full Name</th>
			<th>ID</th>
			<th>Email</th>
			<th># of Stocks</th>
			<th>Total Value of Stocks
			<button {{action sort}} class="sort" >{{sort_arrow sortAscending}}</button>

			</th>
			<th>Controls</th>
		</tr>
	  {{#each portfolio in controller}}
		<tr>
	    {{view HelloEmber.PortfolioInMainView contentBinding="contact"}}
		</tr>
	  {{/each}}
		<tr>
			<td></td>
			<td></td>
			<td></td>
			<td></td>
			<td style='text-align:right;'>Total: ${{decimal total_value}}</td>
			<td></td>
		</tr>
	  </table>
	{{/if}}
		
</div>


